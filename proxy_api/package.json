{
  "name": "nextbus-proxy",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:web": "cd .. && trunk build --release",
    "deploy": "wrangler deploy",
    "deploy:web": "npm run build:web && wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240405.0",
    "typescript": "^5.4.3",
    "wrangler": "^4.73.0"
  }
}
